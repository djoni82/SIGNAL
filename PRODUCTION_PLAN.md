# План вывода в продакшен (SignalPro Ultra)

Этот документ описывает шаги по финальному тестированию и полноценному запуску бота.

## 📊 Текущий статус
- **Ядро**: Ultra Mode (ML + Smart Money) полностью интегрировано.
- **WebSocket**: Оптимизирован (Combined Streams), исправлены ошибки маппинга символов.
- **Стабильность**: Настроен автозапуск macOS (LaunchAgent) 24/7.
- **Версия**: 2.0.0-ultra

---

## 🛠 Фаза 1: Финальная верификация (24-48 часов)
Цель: Убедиться в корректности данных и отсутствии утечек памяти.

### Основные проверки:
1. **Качество Smart Money**:
   - Проверить логи на наличие `SM=+0.XX (websocket)`.
   - Убедиться, что при резких скачках Funding Rate или ликвидациях бот дает соответствующий буст.
2. **Стабильность соединения**:
   - Проверка лога на отсутствие частых `WS Combined Stream Error`.
   - Проверка потребления RAM (не должно превышать 500MB).
3. **Авто-старт**:
   - Перезагрузить ноутбук и проверить, поднялся ли бот самостоятельно (`launchctl list | grep signalpro`).

---

## ⚙️ Фаза 2: Тонкая настройка (Fine-tuning)
Цель: Оптимизация количества и точности сигналов.

- **Порог уверенности**: 
  - Сейчас стоит `ultra_min_confidence: 0.55`.
  - Если сигналов слишком мало (< 2 в день), снизить до `0.50`.
  - Если сигналов много, но они ложные, поднять до `0.60`.
- **ML Retraining**:
  - Каждые 7 дней запускать `setup_auto_retraining.sh` для обучения моделей на свежем рынке.

---

## 🚀 Фаза 3: Полный продакшен
Цель: Переход в режим реальной торговли.

1. **Режим исполнения**:
   - Включить авто-открытие сделок в Telegram (если планируется).
   - Настроить лимиты на одну сделку (Risk Management).
2. **Масштабирование**:
   - Расширить список пар до 90+ в `src/core/settings.py`.
3. **Мониторинг**:
   - Использовать Dashboard для отслеживания результатов в реальном времени.

---

## 🧪 Инструкция для ручного теста (Sanity Check)

Запустите этот скрипт, чтобы убедиться, что WebSocket живой и отдает данные прямо сейчас:
```bash
python3 -c "
import sys
sys.path.insert(0, '.')
from src.strategies.binance_ws import BinanceWSClient
import asyncio
async def test():
    client = BinanceWSClient(['BTC/USDT'])
    await client.start()
    print('Waiting for data...')
    await asyncio.sleep(5)
    print('Metrics:', client.get_metrics('BTC/USDT'))
    await client.stop()
asyncio.run(test())
"
```
